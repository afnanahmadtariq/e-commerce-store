# =================================================
# Frontend Environment Configuration (Render)
# =================================================
# NOTE: For a static site, these often need to be built-in at build time
# or provided via `environment.prod.ts` replacement.
# If using the Docker container (nginx), these might be less relevant 
# unless you are injecting them into the JS at runtime.

# The URL of your API Gateway
API_URL=https://your-gateway-service.onrender.com/api
